server {
    listen       8080;
    server_name  _;

    root   /usr/share/nginx/html;
    index  index.html;

    include /opt/app-root/etc/nginx.default.d/*.conf;

    location / {
        try_files $uri $uri/ =404;
    }
}